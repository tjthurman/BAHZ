context("prep_init_list")

# test argument checking
# bad prior file
# bad tails
# bad chains

# Functionality
# give it an unitializable one, make sure if fails
# give it a good one, make sure it is the same as the reference (with set.seed)
