---
output:
  md_document:
    variant: markdown_github
---

<!-- README.md is generated from README.Rmd. Please edit that file -->

```{r, echo = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  fig.path = "README-"
)
write.
```

# cap

The goal of cap is to ...

## Example

for stan models, can print them in readable form with

cat(model$model)

Can write them to a file to be edited yourslef with:
# definitely write.file from reader, maybe write from base?
write

Note:
Current dependencies:
yaml
rstan (ONLY IF I WRITE A WRAPPER FUNCTION)
assert_that
dplyr (currently in sim_data_from_cline, could possibly re-write to avoid depencency)
magrittr (currently in sim_data_from_cline, could possibly re-write to avoid depencency), or could:
@importFrom magrittr %>%. 




