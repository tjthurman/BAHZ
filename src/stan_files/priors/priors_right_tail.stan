int p_dist_tauR; //which prior distribution to use for pmax
real p_tauR_1; // the first parameter of the prior distribution for tauR
real p_tauR_2; // the second parameter of the prior distribution for tauR
real p_deltaR_1; // the first parameter of the prior distribution for deltaR
real p_deltaR_2; // the second parameter of the prior distribution for deltaR
